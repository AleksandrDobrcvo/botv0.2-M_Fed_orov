{
  "name": "tronix-game-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-cloudflare.mjs",
    "test": "node --test tests/cloudflare-worker.test.mjs",
    "deploy": "npm run build && npx wrangler deploy"
  }
}
